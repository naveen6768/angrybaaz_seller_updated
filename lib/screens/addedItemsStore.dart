import 'package:flutter/material.dart';

class AddedProductsStore extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Sellers Store'),
      ),
    );
  }
}
